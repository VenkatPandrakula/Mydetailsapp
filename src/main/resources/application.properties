# DataSource configuration
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/new_schema
spring.datasource.username=root
spring.datasource.password=root

# Hibernate properties
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Additional properties (optional)
# spring.datasource.initialization-mode=always  # Initialize the database schema (use 'always' for development, 'never' for production)
# spring.datasource.hikari.connection-test-query=SELECT 1   # Test query for connection health check


